# =============================================================================
# Copy to .env.local for local dev. For Vercel: set these in the dashboard only.
# Never commit .env or .env.local — they are in .gitignore.
# =============================================================================

# Privy (wallet auth)
VITE_PRIVY_APP_ID=

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# QuickNode Ethereum RPC (Uniswap quotes, token price)
VITE_QUICKNODE_RPC_URL=

# QuickNode Hyperliquid (for Hyperliquid trigger/filter blocks)
# HTTP: historical/on-demand; WSS: real-time streaming
VITE_QUICKNODE_HYPERLIQUID_HTTP_URL=
VITE_QUICKNODE_HYPERLIQUID_WS_URL=
# Optional: defaults to public api.hyperliquid.xyz/info if unset
VITE_QUICKNODE_HYPERLIQUID_INFO_URL=

# Optional: debug log ingest (only used when URL is set)
VITE_DEBUG_INGEST_URL=
VITE_DEBUG_SESSION_ID=
