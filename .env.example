# =============================================================================
# Copy to .env.local for local dev. For Vercel: set these in the dashboard only.
# Never commit .env or .env.local — they are in .gitignore.
# =============================================================================

# Privy (wallet auth)
VITE_PRIVY_APP_ID=
# Key quorum ID from Privy Dashboard (Authorization keys → New key → Register key quorum). Required for "trade on my behalf" / server signers.
VITE_PRIVY_KEY_QUORUM_ID=

# Server-only / setup: Privy app secret (Dashboard → your app → App secret). Needed to run npm run setup:privy-quorum and for Vercel.
# PRIVY_APP_SECRET=

# Server-only (Vercel env): for automatic swap execution (no approval popup).
# PRIVY_APP_ID=   (same as VITE_PRIVY_APP_ID)
# PRIVY_AUTH_PRIVATE_KEY=   (contents of .privy-auth-key.base64)

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# QuickNode Ethereum RPC (Uniswap quotes, token price)
VITE_QUICKNODE_RPC_URL=

# QuickNode Hyperliquid (for Hyperliquid trigger/filter blocks)
# HTTP: historical/on-demand; WSS: real-time streaming
VITE_QUICKNODE_HYPERLIQUID_HTTP_URL=
VITE_QUICKNODE_HYPERLIQUID_WS_URL=
# Optional: defaults to public api.hyperliquid.xyz/info if unset
VITE_QUICKNODE_HYPERLIQUID_INFO_URL=

# Telegram (server-only): bot token for Send Telegram / Get Telegram. Set in Vercel env for production.
# TELEGRAM_BOT_TOKEN=

# Optional: for local dev with `vite` — proxy /api to your deployed Vercel URL so Telegram blocks work
# (Or run `vercel dev` instead of `vite` to get API routes locally)
# VITE_API_PROXY_TARGET=https://drag-and-swap.vercel.app

# Optional: debug log ingest (only used when URL is set)
VITE_DEBUG_INGEST_URL=
VITE_DEBUG_SESSION_ID=
