/**
 * Fixed handle IDs for execution flow. All edges are execution-only:
 * one handle in (exec-in) and one handle out (exec-out) per block.
 */
export const EXEC_IN_HANDLE = 'exec-in'
export const EXEC_OUT_HANDLE = 'exec-out'
